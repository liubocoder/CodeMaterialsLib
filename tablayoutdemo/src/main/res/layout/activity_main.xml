<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:id="@+id/main_content"
              android:orientation="vertical"
              android:background="@color/comm_red_dark"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:fitsSystemWindows="true"
              tools:context=".MainActivity">

    <android.support.design.widget.TabLayout
            android:id="@+id/tab_layout"
            tools:background="@color/comm_red"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            app:tabMode="scrollable"
            app:tabTextColor="@color/comm_white_60"
            app:tabSelectedTextColor="@color/comm_main"
            app:tabIndicatorColor="@color/comm_main"
            app:tabIndicatorHeight="3dp"
    />

    <android.support.v4.view.ViewPager
            android:id="@+id/container"
            tools:background="@color/comm_main"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_height="0dp"/>

</LinearLayout>